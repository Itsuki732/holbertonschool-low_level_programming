#include "main.h"

/**
* _isdigit - Entry point
* @c: character to check
*
* Return: 1*0 if c is lowercase, 1 otherwise
*
*/

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
		{
		return (0);
	}
	return (0);
}
